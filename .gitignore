# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Security scan results
results.sarif

# TFLint
.tflint.hcl
.tflint.d/

# Python
.venv/
__pycache__/
*.pyc

# OS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo

# Build artifacts
publish_tmp/
validation_error.txt

#Agent
.agent

# AI assistant context — may contain client names / internal context.
# Keep OUT of this public repo. Commit a sanitized version explicitly if needed.
CLAUDE.md
CLAUDE.local.md
.claude/

# Local credentials / service-account keys — never commit (public repo)
*.json.key
*-key.json
*-sa.json
credentials.json
application_default_credentials.json
*.pem
*.p12
